# GD-NOTIFICATION_SYS
This project is a notification system that sends real-time NBA games score and details for the matches happening on that day, to the subscribed users via SMS/Email. It uses an external API to fetch live game scores and sends updates with the use of AWS services.
